// Create a program to declare two integer variables, assign them values, and display their values.
#include<stdio.h>
int main()
{
    int a, b;
    a = 2;
    b = 3;
    printf("a = %d\nb = %d\n", a, b);
    return 0;
}