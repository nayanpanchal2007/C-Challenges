// Create a program to calculate product of two floating points numbers.
#include<stdio.h>
int main()
{
    float a, b;
    printf("Plese enter any two numbers: ");
    scanf("%f %f", &a, &b);
    printf("The product of the given numbers is %f", a * b);
    return 0;
}