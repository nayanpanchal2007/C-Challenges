// Define a Car structure with fields for make, model, year, and color.
#include <stdio.h>

struct car {
    char comapany[20];
    char model[10];
    int year;
    char color[10];
};

typedef struct car car;

int main() {
    
    return 0;
}